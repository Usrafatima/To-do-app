/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com', 'i.pravatar.cc'],
  },
  output: "standalone",
  /* config options here */
};

module.exports = nextConfig;
